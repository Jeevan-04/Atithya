name: atithya
description: >
  आतिथ्य — A royal luxury hospitality platform for India's finest heritage
  estates, palaces, and sanctuaries. Crafted for elite travellers seeking
  curated stays, bespoke concierge service, and immersive cultural journeys.

# Author:  Jeevan Naidu
# GitHub:  https://github.com/Jeevan-04
# Contact: jeevannaidu04@gmail.com
# License: Proprietary — All rights reserved © 2025-2026 Jeevan Naidu

publish_to: 'none'

# Version history:
#   1.0.0+1  — Initial release: auth, estates, booking, itineraries, payment
#   1.1.0+2  — Calendar fusion, booking cards, card formatter, expiry validation
#   1.2.0+3  — Phantom role, admin estate management, feature expansion
version: 1.2.0+3

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  riverpod: ^3.2.1
  flutter_riverpod: ^3.2.1
  google_fonts: ^8.0.2
  lottie: ^3.3.2
  rive: ^0.14.4
  animations: ^2.1.1
  panorama: ^0.4.1
  model_viewer_plus: ^1.10.0
  camera: ^0.12.0
  uuid: ^4.5.3
  panorama_viewer: ^2.0.7
  flutter_animate: ^4.5.2
  audioplayers: ^6.6.0
  http: ^1.6.0
  shared_preferences: ^2.5.4
  intl: ^0.20.2
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  smooth_page_indicator: ^2.0.1
  flutter_staggered_grid_view: ^0.7.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.1
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/pin1_exterior.jpg
    - assets/images/pin2_interior.jpg
    - assets/images/pin3_details.jpg
    - assets/images/pin4_suite.jpg
    - assets/images/pin5_arch.jpg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
